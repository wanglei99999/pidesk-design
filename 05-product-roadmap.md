# 产品路线

本路线描述 PiDesk 的能力演进顺序，不代表固定发布日期。每个阶段都应由具体场景验证，未通过验证的能力不提前扩张。

## 阶段 0：项目基础

### 目标

- 建立产品目标、场景、任务、ADR 和 Changelog 的职责边界；
- 建立 GitHub 与多电脑同步流程；
- 使用公开资料和模拟数据准备首个场景；
- 为每个任务写清范围、风险、验收和下一步。

### 完成条件

- 当前任务、分支和下一步可以从仓库恢复；
- 产品决定与技术决定分开记录；
- 首个场景有明确输入、输出和验收标准。

## 阶段 1：验证 Agent 运行闭环

首个任务是 [TASK-001：验证 Pi 办公 Agent 最小运行闭环](tasks/TASK-001-file-organization.md)。它使用文本文件批量重命名作为受控实验，不作为 PiDesk 的首个完整产品功能。

### 目标

- 对比 Pi SDK 与 RPC 在该场景中的接入边界；
- 先验证目录读取和文件分析，再加入重命名计划；
- 实现“生成计划—用户确认—执行—记录结果”的闭环；
- 明确 Pi 适配层的最小职责；
- 形成第一条经过实现验证的开发规范。

### 完成条件

- 模拟目录中的文本文件可以完成批量重命名；
- 冲突、越界、取消和执行前文件变化都有确定处理；
- 用户能看到准确的操作计划和执行结果；
- SDK、RPC 或其他接入方式有基于代码实验的结论，并写入 ADR。

## 阶段 2：完成首个产品纵向切片

首个用户可感知的产品切片是“工作区中的文档资料处理与生成”。具体文档类型和样例在进入开发前单独确认。

### 目标

- 以任务和工作区承载用户目标、引用资料、对话、状态和产物；
- 用户通过自然语言要求 Agent 整理资料并生成结构化文档；
- 复用阶段 1 验证过的 Pi 接入、Tool、事件、确认和记录能力；
- 支持预览生成物，并在同一任务中继续提出修改；
- 明确 Pi 会话、PiDesk 任务和产物保存的职责边界；
- 形成后续文档、表格、调研等场景可以复用的产品入口。

### 完成条件

- 用户可以在一个工作区内完成“引用资料—执行—预览产物—继续修改—保存结果”；
- 任务能够关联输入、执行记录和最终产物；
- 继续修改不需要重新描述全部上下文；
- 失败、取消和不完整结果有明确状态；
- 最终结果是可检查的文件或结构化产物，不只是一段聊天文字。

## 阶段 3：扩展基础办公场景

候选顺序：

1. 会议材料整理；
2. 文档生成与修改；
3. 表格分析；
4. 演示文稿生成；
5. 网页调研与结果整理。

实际顺序由复杂度、复用价值、用户价值和学习价值决定。

### 同步建设的公共能力

- 任务状态和运行记录；
- 工作目录和产物管理；
- Tool 注册与参数校验；
- 操作预览、确认和取消；
- 错误展示和可恢复执行；
- 模型、耗时和用量记录。

### 完成条件

- 至少两个场景能用公开或模拟数据完整运行；
- 公共能力被不同场景复用；
- 正常路径和适用的失败路径有测试；
- 结果可以检查，不只是一段聊天文字。

## 阶段 4：接入办公服务

### 目标

- 先接入一个只读服务；
- 建立认证和凭据隔离；
- 将查询与修改操作分开；
- 记录外部调用；
- 只在只读流程稳定后增加写操作。

### 完成条件

- Agent 不能获得超过当前用户授权的能力；
- 凭据不进入模型上下文和普通日志；
- 外部写入可预览、可确认并防止重复执行；
- 限流、超时、授权过期和接口错误有明确结果。

## 阶段 5：沉淀可复用场景能力

只有重复步骤已经在多个任务中稳定出现时，才考虑整理为 Skill、模板或公共 Tool。

稳定的角色、默认上下文、Skill 和工具组合可以进一步形成助理或专家，但不能因此自动扩大权限。

### 完成条件

- 新场景可以组合既有能力，不必复制整套流程；
- 能力说明包含适用条件、依赖、输入、输出和失败处理；
- 更新后有固定样例验证；
- 复用不会自动扩大 Tool 权限。

## 阶段 6：自动任务与远程入口

### 前提

- 对应手动任务已经稳定；
- 任务具有明确的超时、重试和幂等策略；
- 无人值守时的权限边界已经验证。

### 完成条件

- 自动任务可暂停、恢复和手动补跑；
- 重复触发不会造成重复副作用；
- 每次运行有独立记录；
- 远程入口能正确识别用户与授权范围。

## 阶段 7：多 Agent

只有满足以下条件才进入：

- 单 Agent 方案已经稳定；
- 任务确实存在可以独立检查的分工；
- 各角色的输入、输出和失败边界明确；
- 与单 Agent 相比有可测量的质量、速度或成本收益。

多 Agent 是解决复杂协作问题的方案，不是产品成熟度指标。

## 每个场景的交付物

- 场景说明；
- 开发任务和进度；
- 必要的技术决策；
- 测试样例和验证结果；
- 使用说明；
- 实际学习记录；
- 经实现验证后新增或修改的规范；
- 用户可检查的最终产物。

## 完成标准

不以页面数量、模型数量、Skill 数量或是否使用多 Agent 判断完成。真正的标准是：目标用户能完成指定工作，结果可检查，失败可处理，风险受控制。
