# Pi 和 Agent 学习记录方式

## 学习方法

不按源码目录从头读到尾。每做一个场景，沿着实际请求去找 Pi 代码。

统一采用以下顺序：

1. 写清场景的输入、输出和风险；
2. 用 Pi 跑通最小任务；
3. 找到请求经过的主要类和函数；
4. 记录状态和事件怎样变化；
5. 增加一个业务 Tool 或 Skill；
6. 补取消、失败和恢复；
7. 写测试；
8. 总结本场景暴露的问题。

## 场景与学习内容

| 场景 | Pi 内容 | Agent 内容 |
|---|---|---|
| 文件整理 | Tool、AgentSession、文件工具 | 工具选择、参数校验、写入确认 |
| 会议纪要 | Skill、ResourceLoader | 提示词、结构化结果、长文本 |
| Word/PPT | Extension、Tool 输出 | 文件生成、多轮修改、产物校验 |
| Excel 分析 | 命令和自定义 Tool | 计算交给程序、结论交给模型 |
| 网络调研 | Extension、事件 | 搜索、来源、上下文控制 |
| 内部系统 | SDK、Extension | 认证、权限、外部副作用 |
| 自动任务 | RPC、Session | 长任务、重试、恢复 |
| 专家 | Skill、系统提示词 | 角色、方法和工具组合 |
| 多 Agent | Orchestrator 思路 | 分工、隔离、汇总、成本 |

## 每次学习记录模板

### 场景

- 当前实现的业务任务：
- 为什么适合使用 Agent：
- 哪些步骤不用模型：

### 请求流程

- 入口：
- 会话创建：
- 模型调用：
- Tool 调用：
- 事件：
- 保存：
- UI 消费：

### 阅读的 Pi 文件

列出文件路径、关键类型和作用。

### 实际问题

- 模型理解错误：
- Tool 参数错误：
- 状态问题：
- 中断和恢复：
- 权限问题：
- 性能和成本：

### 结论

- 可以直接复用的 Pi 设计：
- 需要包装的部分：
- 不适合办公产品的部分：
- 修改的开发规则：

## 学习成果的边界

可以公开：

- Pi 公共源码分析；
- 通用 Agent 设计；
- 使用模拟数据的演示；
- 不含公司细节的失败案例；
- 经公司确认可公开的效率数据。

不能默认公开：

- 公司代码；
- 内部接口；
- 凭据和网络地址；
- 真实业务数据；
- 员工和客户信息；
- 未公开的产品计划；
- 公司专用提示词和 Skill。

## 与现有学习资料配合

`docs/study` 已经覆盖 Pi 的架构、Agent、AI、coding-agent、运行模式、扩展、测试和稳定性。

建议按场景选择阅读：

- 第一个 Tool：先读 `01-agent.md` 和 `03-coding-agent-core.md`；
- 模型配置：读 `02-ai.md`；
- 产品接入：读 `04-modes-and-tui.md` 中 SDK/RPC 相关部分；
- Skill 和 Extension：读 `05-extensions.md`；
- 场景测试：读 `06-testing.md`；
- 中断、恢复和成本：读 `07-robustness-and-cost.md`。
