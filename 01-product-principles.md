# 产品设计原则

产品目标、范围和优先顺序以 [product-decisions.md](product-decisions.md) 为准。本文件只记录设计和实现过程中长期遵守的原则。

## 从具体任务出发

先确定用户要完成的办公任务，再判断需要模型、普通程序、Tool、Skill 或连接器。不能为了展示技术，先决定要做多 Agent、知识库或自动化平台。

## 市面产品只提供场景参考

市面产品用于确认哪些办公问题已经存在、用户如何输入、产品交付什么结果。PiDesk 不通过黑盒测试猜测产品内部实现，也不照抄界面、回答风格和交互细节。

## 每个场景都增加公共能力

场景不是彼此独立的演示。完成一个场景时，应明确它为后续场景增加了哪些可以复用的能力，例如：

- 工作目录和文件访问；
- Tool 注册和参数校验；
- 操作预览和用户确认；
- 任务状态和执行记录；
- 产物保存和结果校验；
- 连接器和凭据边界。

没有复用价值的场景也可以实现，但需要说明它解决的独立问题。

## Agent 与普通程序分工

模型适合处理：

- 理解自然语言和非结构化资料；
- 分类、提取和总结；
- 生成候选内容；
- 在多个合理方案中提供建议。

普通程序必须负责：

- 权限和工作目录边界；
- 参数、Schema 和文件名校验；
- 任务状态和数据保存；
- 冲突、重复执行和幂等；
- 审批和外部写入；
- 可确定检查的结果。

模型输出不能直接成为权限或执行许可。

## 先预览，再产生副作用

修改文件、写入外部系统、发送消息、删除数据和执行命令前，应展示具体动作、目标和影响。用户确认的内容必须与程序最终执行的内容一致。

## 先完成手动任务

同一个场景先由用户主动发起并验证，再考虑定时执行、远程入口和无人值守。自动执行不能成为第一版功能。

## 先完成单 Agent

单 Agent 能稳定完成任务后，再判断是否需要多 Agent。只有任务确实可以分工，并且质量或时间有可测量改善时才采用多 Agent。

## 先验证，再形成规范

安全底线可以提前确定，具体实现方法必须经过场景代码和测试验证。未经实现验证的想法只能标记为候选方案，不能写成已经确认的技术事实。

## 结果必须可检查

场景不能只返回一段看起来合理的聊天文字。应尽量提供结构化结果、文件产物、变更预览、执行记录或可以重复运行的检查。

## 当前待验证问题

- 第一版使用 coding-agent SDK、RPC 还是更底层的 agent-core；
- 长期产品入口采用 CLI、桌面界面还是 Web 界面；
- 任务和会话由 PiDesk 保存到什么程度；
- Skill、Extension 和连接器在产品中的具体边界；
- 是否需要 MCP；
- 何时需要独立后台任务或多 Agent。
