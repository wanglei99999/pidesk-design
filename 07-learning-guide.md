# Pi 与 Agent 学习指南

本文件定义学习方法，不冒充实际学习记录。真实结论应来自场景实现和代码实验，并记录在对应任务或后续 `learning/` 文档中。

## 学习方法

不按源码目录从头读到尾。每完成一个场景，沿实际请求追踪 Pi：

1. 写清场景输入、输出、风险和验收；
2. 用最小实验比较可选接入方式；
3. 找到请求经过的主要类型和函数；
4. 记录模型、Tool、事件和状态如何流转；
5. 增加一个场景 Tool；
6. 验证适用的取消、失败、确认和恢复路径；
7. 用确定性测试固定系统行为；
8. 总结可以复用、需要包装和不适合采用的设计。

## 当前学习基线

PiDesk 当前分析基于 Pi `0.80.10` 和提交 `eb8dd587e780b5393f53635002004cb2b5ef8f92`。固定资料见 [资料来源](sources.md)。升级 Pi 后，需要重新验证相关结论。

## 场景与候选学习内容

下表用于安排阅读方向，不代表已经确定的实现方式。

| 场景 | Pi 候选内容 | Agent 学习内容 |
|---|---|---|
| 运行闭环验证（文件重命名实验） | SDK、RPC、AgentSession、Tool | 工具选择、结构化计划、参数校验、写入确认 |
| 工作区文档处理 | Session、ResourceLoader、Tool 输出 | 任务上下文、产物、多轮修改、状态边界 |
| 会议材料整理 | Skill、ResourceLoader、Session | 长文本、信息提取、结构化结果 |
| 文档与演示文稿 | Extension、Tool 输出 | 文件生成、多轮修改、产物校验 |
| 表格分析 | 自定义 Tool、命令执行 | 计算与推理分工、结果证据 |
| 网页调研 | Extension、事件 | 搜索、来源、上下文控制 |
| 办公服务 | SDK 或 RPC、Extension | 认证、权限、外部副作用 |
| 自动任务 | RPC、Session | 长任务、重试、恢复 |
| 稳定工作方法 | Skill、系统提示词 | 方法固化、工具组合、回归验证 |
| 多 Agent | Orchestrator | 分工、隔离、汇总、成本 |

## 每次学习记录

真正完成代码实验后，再创建记录，至少包含：

### 场景与问题

- 当前实现的任务；
- 本次要回答的技术问题；
- 为什么需要模型；
- 哪些步骤由确定性程序完成。

### 请求流程

- 产品入口；
- Pi 接入方式；
- 会话与模型调用；
- Tool 调用；
- 事件和状态；
- 保存与恢复；
- 产品层如何消费结果。

### 阅读与实验

- Pi 版本和提交；
- 阅读的文件、关键类型及作用；
- 最小实验代码；
- 运行命令和实际结果；
- 与其他候选方案的比较。

### 问题与结论

- 模型理解或结构化输出问题；
- Tool 参数和执行问题；
- 状态、中断与恢复问题；
- 权限、性能和成本问题；
- 可以直接复用的设计；
- 需要适配的设计；
- 不采用的设计及原因；
- 是否产生 ADR 或开发规则修改。

## 记录原则

- 只记录已经观察到的事实和可复现实验；
- 将事实、推断和决定分开；
- 结论必须带 Pi 版本或提交；
- 未验证的内容标为候选；
- 不为凑学习记录而扩大当前任务范围。
