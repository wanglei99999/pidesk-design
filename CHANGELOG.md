# 更新记录

本文件记录用户可以感知的重要变化。任务进度、学习笔记、内部重构和普通文档修正不写入这里。

版本发布前，所有变化先记录在 `[Unreleased]`。完成一个可独立使用的场景后发布 `0.x` 版本。

日常开发只读取和更新 `[Unreleased]`。历史版本仅在发布、排查回归或编写迁移说明时读取。项目保持一个根目录 Changelog；只有出现多个独立版本的子产品时，才考虑拆分。

## [Unreleased]

### Added

### Changed

- 公开产品名称统一为 DeskBuddy，`pidesk` 保留为代码标识。
- 明确 Coding Agent 是基础能力，并将只读代码工作区确定为首个产品纵向切片。

### Fixed

### Removed
