# 项目目标和范围

## 项目定位

这个项目不是 WorkBuddy 的复制品，也不是 Pi 的换皮。

更准确的说法是：

> 参考成熟办公 Agent 的业务场景，使用 Pi 提供的模型、Agent、工具和会话能力，开发适合公司内部工作的办公助手。

用户关心的是工作有没有做完，开发者关心的是任务是否安全、稳定、可检查。两者都需要满足。

## 三个目标

### 公司目标

- 减少重复办公操作；
- 缩短资料整理、数据分析和文档制作时间；
- 连接现有内部系统，不要求员工反复复制数据；
- 对修改数据、发送消息等操作保留人工确认；
- 能记录执行过程，出现问题时可以追查。

### 学习目标

- 理解一次请求如何经过 `coding-agent`、`agent-core` 和 `pi-ai`；
- 掌握 Tool、Skill、Extension、Session、RPC 等 Pi 机制；
- 理解 Agent 的上下文、工具选择、中断、恢复和结果校验；
- 了解办公 Agent 在权限、定时执行和企业系统接入方面的问题；
- 能根据业务场景判断应该使用 Agent、普通程序还是两者配合。

### 个人成果

- 保留不涉及公司机密的架构和技术记录；
- 使用模拟数据制作公开演示；
- 说明 Pi 提供了什么、自己补充了什么；
- 用真实问题讲清设计取舍、失败案例和改进过程；
- 未经公司允许，不公开内部数据、接口、提示词或源码。

## 工作原则

### 从工作任务出发

不先决定要做“多 Agent 平台”或“企业知识库”。先找到员工每天实际做的工作，再判断需要哪些功能。

### WorkBuddy 只提供场景参考

我们使用它的官方文档确认场景和用户结果，不通过黑盒测试猜测内部实现，不把模型回答风格当成产品设计。

### Pi 负责 Agent 运行

Pi 适合处理模型接入、Agent 循环、工具调用、会话、技能和扩展。公司身份、内部系统权限、审批、审计、定时任务和办公文件能力需要单独设计。

### Agent 不替代确定性程序

需要理解语义、判断资料和组织内容时使用模型。权限判断、数据保存、任务状态、文件冲突、审批和系统写入由普通程序控制。

### 先完成单 Agent

单 Agent 能稳定完成任务后，再判断是否需要多 Agent。多 Agent 必须能证明质量或速度上的收益，不能只为展示。

### 先完成手动任务

同一个任务先由用户手动发起并验证，再增加定时执行或远程触发。无人值守任务的风险更高。

## 暂不确定的内容

以下事项要等公司需求明确后决定：

- 桌面应用、Web 应用还是接入现有系统；
- 首批业务部门；
- 第一批场景；
- 本地部署、内网部署还是混合部署；
- 是否允许调用公网模型；
- 使用 SDK 还是 RPC；
- 是否需要 MCP；
- 是否需要多 Agent；
- 哪些内容可以作为公开作品。
