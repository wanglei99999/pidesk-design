# 办公 Agent 开发规范草案

## 状态

这是第一版草案。后续只能根据真实场景和实现结果升级，不能把未经验证的设想写成强制规则。

## 场景

- 场景必须对应真实岗位和工作；
- 必须写清当前人工步骤；
- 必须有具体输入样例和输出样例；
- 必须说明完成标准；
- 必须区分模型质量和系统正确性；
- 没有试用人员的场景不进入首批开发。

## Tool

- 一个 Tool 只负责一类明确操作；
- 名称和描述应让模型知道何时使用；
- 参数使用明确 Schema 校验；
- 读取和修改操作尽量分开；
- 修改、删除、发送和执行命令必须标记风险；
- 支持取消的 Tool 必须响应取消信号；
- 外部调用要设置超时；
- 输出过大时要截断或保存为文件；
- 失败结果必须包含可处理的信息；
- 不能依赖模型自行遵守权限。

### Tool 风险分类

| 类别 | 示例 | 默认处理 |
|---|---|---|
| 本地只读 | 读取授权文件 | 工作目录内可执行 |
| 外部只读 | 查询知识库、CRM | 使用员工已有权限 |
| 本地写入 | 生成或修改文件 | 限制目录，必要时确认 |
| 外部写入 | 创建日程、更新 CRM | 执行前确认 |
| 对外发送 | 邮件、群消息 | 执行前确认并预览 |
| 删除和命令 | 删除文件、运行程序 | 高风险确认或沙箱 |

## Skill

- Skill 说明必须写清使用时机；
- 一个 Skill 对应一种稳定工作方法；
- Skill 不应隐藏必须由程序保证的安全规则；
- 依赖的 Tool、脚本和参考资料要明确；
- 输入、输出和失败处理要写清楚；
- 版本更新后运行固定样例；
- Skill 不能自动获得超出当前用户的权限；
- 公司 Skill 要有负责人和变更记录。

## 连接器

- 凭据由专门的凭据存储管理；
- 不把 Access Token、Refresh Token 或密码放进提示词；
- 读操作和写操作分开；
- 使用最小权限；
- 记录外部系统、用户、操作、时间和结果；
- 处理限流、超时和授权过期；
- 写操作支持幂等或重复检查；
- 断开连接后立即停止继续使用凭据。

## 任务

建议至少区分：

- 新建；
- 运行中；
- 等待确认；
- 已暂停；
- 已完成；
- 已失败；
- 已取消。

每次状态变化应有明确原因。模型文字不能直接代替任务状态。

任务应支持：

- 取消；
- 失败后继续；
- 查看使用的模型和 Tool；
- 查看输入和生成文件；
- 记录用户确认；
- 区分同一任务的多次运行。

## 工作目录和文件

- 每个任务有明确的工作目录；
- 默认不访问工作目录外文件；
- 重要文件修改前生成备份或新文件；
- 批量操作先提供预览；
- 目标文件冲突时不默认覆盖；
- 生成文件记录来源任务和运行次数；
- 临时文件与最终产物分开；
- 文件解析失败时保留原文件，不静默跳过。

## 权限和审批

- 权限由程序判断，不由模型判断；
- 审批要展示具体动作、对象和影响；
- 用户拒绝后，Agent 可以改用只读或生成预览的方式继续；
- 远程和自动任务采用更严格的规则；
- 完全放开权限只用于隔离、可恢复的环境；
- 所有高风险操作保留审计记录。

## 模型

- 产品不能依赖某个模型特有的回答格式；
- Tool 参数必须经过校验；
- 模型输出不能直接作为权限结果；
- 对关键结果增加普通程序校验或人工验收；
- 切换模型后应运行场景样例；
- 记录模型、版本、耗时和用量，便于排查质量变化。

## 测试

每个场景至少包含：

- 正常样例；
- 缺少输入；
- 格式错误；
- Tool 超时；
- 用户取消；
- 用户拒绝审批；
- 重复执行；
- 文件或数据冲突；
- 无权限；
- 模型输出不符合 Schema。

Agent 测试优先使用 faux provider 或固定响应，不在自动测试中调用真实付费模型。真实模型测试单独执行并记录模型和时间。

## 自动任务

- 手动任务验证通过后才能创建自动任务；
- 自动任务必须有最大运行时间；
- 必须限制并发和重复触发；
- 每次运行产生独立记录；
- 失败后通知负责人；
- 涉及外部写入和发送时采用专门审批策略；
- 任务停用后不能继续调度。

## 多 Agent

- 先定义各 Agent 的职责、输入和输出；
- 子任务必须可以独立检查；
- 不共享不必要的上下文；
- 汇总结果时处理冲突和缺失；
- 记录每个 Agent 的模型调用和成本；
- 必须和单 Agent 方案比较后再决定是否使用。
