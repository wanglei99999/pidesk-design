# 办公 Agent 开发规范

## 规则状态

本文分为两类内容：

- **安全基线**：可以直接执行，不依赖具体框架；
- **待验证实践**：当前是设计候选，必须经过具体任务验证后才能升级为项目规则。

修改规则时，应写明验证它的任务、测试或事故记录，避免把设想当成结论。

## 已接受的安全基线

### Tool 与模型边界

- 权限、目录边界、参数格式和副作用条件由程序校验，不能依赖模型自觉遵守；
- Tool 参数使用明确 Schema，执行时再次校验；
- 读取与修改操作应分开；
- 模型生成的计划不能直接等同于已授权操作；
- 关键结果需要确定性程序校验或人工确认。

### 文件与外部副作用

- 每次任务使用明确的工作目录，默认不访问目录外内容；
- 批量修改、删除、发送和外部写入必须先展示准确计划；
- 用户确认后，只能执行确认过的对象和参数；
- 目标冲突时默认不覆盖；
- 凭据不得进入模型上下文、产物或普通日志；
- 外部操作使用当前用户已有授权，不额外扩大权限。

### 失败与记录

- 失败结果必须包含可处理的信息；
- 外部调用必须有超时；
- 支持取消的操作应响应取消信号；
- 执行结果要区分成功、跳过和失败，不能用一段模型文字代替系统状态；
- 自动化测试不调用真实付费模型，优先使用固定响应或 faux provider。

## 待验证的开发实践

以下内容是首批场景的检查方向，不是已经证明适用于所有场景的强制规则。

### 场景定义

- 说明目标用户、触发条件、现有步骤、输入、输出和完成标准；
- 区分适合模型判断的步骤与确定性程序步骤；
- 准备公开或模拟样例；
- 分开验证模型质量与系统正确性。

### Tool 设计

- 一个 Tool 聚焦一类明确操作；
- 名称、描述和参数帮助模型正确选择；
- 输出过大时截断或保存为产物；
- 修改前后记录足够的信息用于检查和恢复；
- 为适用的写操作设计幂等或重复检查。

### 任务状态

候选状态包括：新建、运行中、等待确认、已暂停、已完成、已失败和已取消。首个纵向切片需要验证：

- 状态是否足够；
- 状态变化由谁负责；
- 取消和失败后继续是否需要持久化；
- 同一任务的多次运行如何区分。

### Skill

只有一种工作方法已经稳定复用时，才考虑整理为 Skill。需要验证：

- 适用时机是否清楚；
- 依赖的 Tool、脚本和资料是否明确；
- 输入、输出和失败处理是否稳定；
- 固定样例能否发现版本更新带来的退化；
- Skill 是否保持原有权限边界。

### 连接器

接入真实服务前需要验证：

- 凭据存储与刷新方式；
- 最小权限是否可实现；
- 查询与修改接口是否能明确分开；
- 限流、超时、授权过期和断开连接如何处理；
- 写操作如何防重并记录结果。

### 自动任务

手动流程稳定后再验证调度、最大运行时间、并发限制、重复触发、失败提醒和无人值守审批。未验证前不把交互式任务直接改成自动任务。

### 多 Agent

只有单 Agent 结果可作为对照时，才验证多 Agent。需要比较职责隔离、上下文成本、失败处理、汇总质量，以及相对单 Agent 的实际收益。

## 场景测试选择

每个场景从下列情况中选择适用项，并在任务文件写明不适用的原因：

- 正常输入；
- 缺少输入或格式错误；
- 模型输出不符合 Schema；
- 用户取消或拒绝确认；
- 文件或数据冲突；
- 执行前对象已经变化；
- 无权限或越过工作目录；
- Tool 超时；
- 重复执行；
- 外部服务限流或授权过期。

真实模型测试与确定性自动化测试分开执行，并记录模型、版本和时间。

## 规则升级方式

1. 在任务中记录问题和候选做法；
2. 用代码和测试验证；
3. 判断它是场景特例还是公共约束；
4. 公共约束写入本文，并链接验证依据；
5. 涉及长期技术取舍时另建 ADR。
