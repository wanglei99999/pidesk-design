# 场景筛选和分析方法

## 先收集真实工作

不要问员工“想要哪些 AI 功能”。更有效的问题是：

- 最近一周反复做过哪些工作？
- 哪件事最耗时间？
- 输入资料来自哪里？
- 中间用了哪些软件？
- 最后要交付什么？
- 哪一步必须由人判断？
- 做错会造成什么影响？
- 是否已经有脚本或系统可以完成一部分？

记录真实案例，不记录空泛愿望。

## 场景记录表

| 字段 | 说明 |
|---|---|
| 场景名称 | 使用工作语言命名，例如“每周销售异常汇总” |
| 使用人员 | 部门、岗位、预计人数 |
| 当前做法 | 按顺序列出人工操作 |
| 使用频率 | 每日、每周、每月或偶发 |
| 单次耗时 | 当前平均耗时 |
| 输入 | 文件、网页、数据库、内部系统 |
| 输出 | 文档、表格、消息、系统记录 |
| 判断工作 | 哪些步骤不能简单写成固定规则 |
| 系统操作 | 查询、创建、修改、删除、发送 |
| 风险 | 数据泄露、误删、误发、错误计算等 |
| 完成标准 | 如何判断结果可以使用 |
| 接口情况 | 是否有 API、MCP、数据库或只能操作页面 |
| 试用人员 | 谁可以参与验证 |

## 筛选条件

优先考虑：

- 使用频率高；
- 人工耗时明显；
- 输入和输出比较稳定；
- 数据可以合法获取；
- 结果容易检查；
- 出错后可以恢复；
- 可以找到真实试用人员。

谨慎考虑：

- 依赖大量隐性经验；
- 结果没有明确对错；
- 直接影响资金、合同或客户；
- 需要修改多个生产系统；
- 只能依赖不稳定的页面自动化；
- 业务部门没有时间参与验收。

## 场景分析模板

复制以下内容创建新的场景文件：

### 1. 基本信息

- 场景名称：
- 使用部门：
- 使用人员：
- 使用频率：
- 当前耗时：
- 预期改善：

### 2. 当前人工步骤

按实际顺序填写，不合并步骤。

### 3. 输入和输出

- 输入来源：
- 输入格式：
- 输出内容：
- 输出格式：
- 保存或发送位置：

### 4. Agent 的工作

- 需要理解什么：
- 需要选择什么：
- 需要生成什么：
- 需要调用哪些工具：

### 5. 普通程序的工作

- 权限判断：
- 数据读取和保存：
- 状态管理：
- 冲突检查：
- 审批：
- 结果校验：

### 6. Pi 的使用

- 使用的包：
- 使用的 Tool：
- 使用的 Skill：
- 使用 SDK 还是 RPC：
- 需要阅读的 Pi 文件：

### 7. 需要新增的部分

- 产品界面：
- Tool：
- Skill：
- 连接器：
- 数据表：
- 后台任务：

### 8. 风险和处理

列出具体风险和对应措施。

### 9. 验收

- 正常案例：
- 边界案例：
- 失败案例：
- 权限案例：
- 模型质量要求：
- 系统行为要求：

### 10. 学习记录

- 学到的 Pi 机制：
- 学到的 Agent 问题：
- 新增或修改的规范：

## 区分模型质量和产品正确性

同一个场景要分别检查：

### 模型质量

- 摘要是否准确；
- 文档表达是否合适；
- 分类和判断是否合理。

模型不同，结果可能不同。需要通过样例、评分或人工检查衡量。

### 产品正确性

- 是否只访问授权数据；
- 是否正确保存任务状态；
- 是否在写操作前确认；
- 是否能取消和恢复；
- 是否处理重复执行和文件冲突；
- 是否留下操作记录。

这部分不能交给模型自由决定，必须由程序和测试保证。
