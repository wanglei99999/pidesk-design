# 实施安排

## 阶段 0：确认公司需求

### 工作

- 访谈目标部门；
- 收集 10～20 个真实任务；
- 记录频率、耗时、输入、输出和风险；
- 找到首批试用人员；
- 选择 2～3 个候选场景。

### 完成条件

- 每个候选场景都有真实使用者；
- 有具体输入样例；
- 有可检查的输出；
- 已确认数据和接口可以使用；
- 业务部门同意参与验收。

## 阶段 1：验证 Pi 接入

### 工作

- 使用 coding-agent SDK 创建最小办公任务；
- 加入一个只读 Tool；
- 订阅任务事件；
- 验证取消、继续和会话保存；
- 确认 PiAdapter 的职责。

### 完成条件

- 一个真实办公任务可以从输入运行到结果；
- 任务可以取消；
- 失败原因可以看到；
- 产品层没有到处依赖 Pi 内部类型；
- SDK 是否继续使用有明确结论。

## 阶段 2：完成第一批基础场景

候选顺序：

1. 文件整理；
2. 会议纪要；
3. Word 文档；
4. Excel 分析；
5. PPT 生成。

实际顺序以公司场景筛选结果为准。

### 同时完成的公共功能

- 任务列表和状态；
- 工作目录；
- 文件和产物查看；
- Tool 注册；
- 操作确认；
- 任务取消和恢复；
- 错误记录；
- 模型及用量信息。

### 完成条件

- 至少两个公司场景可由真实员工试用；
- 正常、失败和取消路径都有测试；
- 文件修改有预览或确认；
- 结果可以检查，不只返回一段聊天文字；
- 有第一版开发规范。

## 阶段 3：连接公司系统

### 工作

- 选择一个只读内部系统；
- 实现认证和凭据托管；
- 将查询和修改工具分开；
- 记录每次外部调用；
- 在试运行稳定后增加写操作。

### 完成条件

- Agent 只能使用当前员工已有权限；
- 凭据不进入模型上下文和日志；
- 写操作需要确认；
- 重复执行不会重复创建数据；
- 接口错误有明确处理方式。

## 阶段 4：Skill 和场景扩展

### 工作

- 将重复工作步骤整理成 Skill；
- 定义 Skill 版本和负责人；
- 建立安装、启用、停用和更新方式；
- 为业务部门提供固定场景入口。

### 完成条件

- 新场景不需要修改 Agent 主流程；
- Skill 说明清楚何时使用；
- Tool 权限不因 Skill 自动扩大；
- 有固定样例验证 Skill 更新。

## 阶段 5：自动任务和远程入口

### 工作

- 先支持定时运行已经验证过的任务；
- 增加超时、并发限制和失败提醒；
- 按需接入企业微信、飞书等入口；
- 区分本地在线、离线和任务排队状态。

### 完成条件

- 自动任务可暂停、恢复和手动补跑；
- 同一时间重复触发不会重复执行；
- 每次运行保留独立记录；
- 无人值守时使用更严格的权限；
- 远程用户身份可以正确对应公司账号。

## 阶段 6：专家和多 Agent

只有满足以下条件才开始：

- 单 Agent 场景已经稳定；
- 任务确实可以分工；
- 可以定义各角色的输入和输出；
- 有单 Agent 结果作为对照；
- 公司接受额外模型成本。

### 完成条件

- 并行任务彼此隔离；
- 汇总规则明确；
- 单个子任务失败不会丢失全部结果；
- 与单 Agent 相比，质量或时间有可测量改善。

## 每个场景的交付物

- 场景需求文件；
- 技术方案；
- 测试样例；
- 使用说明；
- Pi 学习记录；
- Agent 问题记录；
- 新增或修改的规范；
- 业务验收结果。

## 不以这些内容判断完成

- 页面数量；
- 支持多少模型；
- 安装了多少 Skill；
- 是否展示了多 Agent；
- 演示视频是否看起来复杂。

真正的完成标准是：真实员工能完成指定工作，结果可检查，失败可处理，风险受控制。
